package works.merc.keryx.app.ui.theme

import androidx.compose.ui.text.font.FontFamily

/** Roboto (`FontFamily.Default`) is already Android's native UI font — nothing to resolve. */
actual fun appFontFamily(): FontFamily? = null
