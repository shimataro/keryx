# セキュリティポリシー

[English](SECURITY.md)

---

Keryx はローカルファーストのオープンソース RSS リーダーです。フィードデータや、
（利用者が有効にした場合は）クラウドストレージの認証情報を扱うため、セキュリティ
上の報告を重視しており、Keryx の安全性維持に協力してくださる方々に感謝します。

本ドキュメントの技術的な記述（Keryx がデータや認証情報をどう扱うか）はソースコード
で直接確認できます: <https://github.com/shimataro/keryx> 。ただし、以下の対応や開示
に関する取り決めはポリシーであり、コードではありません。アプリが扱うデータの内容と
行き先については [PRIVACY.ja.md](PRIVACY.ja.md) も参照してください。

## サポート対象バージョン

Keryx は 1.0 未満であり、現在も活発に開発中です。
セキュリティ修正は**最新リリース版のみ**が対象で、長期サポートブランチはなく、古いリリースは修正されません。
セキュリティ問題の影響を受けている場合は、まず[リリースページ](https://github.com/shimataro/keryx/releases)から最新版へアップデートしてください。

| バージョン | サポート |
| --- | --- |
| 最新リリース | ✅ |
| それ以前のリリース | ❌ |

## 脆弱性の報告

**セキュリティ脆弱性を、公開の GitHub Issue・Pull Request、その他の公開チャネルで
報告しないでください。** 修正が用意される前に公開されると、利用者を危険にさらします。

代わりに、**GitHub のプライベート脆弱性報告（Private vulnerability reporting）**
を通じて非公開で報告してください:

1. リポジトリの[Securityタブ](https://github.com/shimataro/keryx/security)にアクセス
2. **Report a vulnerability** をクリックし、メンテナーと報告者のみが閲覧できる非公開のアドバイザリを作成

### 報告に含めていただきたい情報

トリアージと修正を迅速に行うため、可能な範囲で以下を含めてください:

- 脆弱性の内容と、想定される影響の明確な説明。
- 影響を受けるバージョン、プラットフォーム（Windows / macOS / Linux）、および
  構成（クラウド同期の有無、利用しているプロバイダなど）。
- 再現手順（可能であれば概念実証コードも）。
- 関連するログ、スタックトレース、スクリーンショット。
- 修正案（あれば）。

## 対応の流れ

- **受領連絡:** 数日以内の受領連絡を目指します。
- **調査:** 内容を調査・確認し、進捗を随時お知らせします。
- **修正と公開:** 修正が用意でき次第リリースし、ご同意いただければアドバイザリ
  にて謝辞を記載します。**協調的開示（coordinated disclosure）**の方針をとって
  いますので、公開の前に修正をリリースするための妥当な猶予をいただけると助かります。

本プロジェクトはボランティアによって維持されているオープンソースであるため、対応
時期はベストエフォートとなります。責任ある報告とご理解に感謝します。

## 対象範囲

Keryx 本体のセキュリティ問題が対象です。例えば以下が該当します:

- OAuth トークンやその他のクラウドストレージ認証情報の不適切な扱い・漏えい。
- 利用者のデータを破損・露出させうる同期／マージ処理の不具合。
- フィード（RSS / Atom）、OPML、HTML の解析における脆弱性。
- フィードコンテンツ・URL・リダイレクトの安全でない扱い。
- 悪意あるフィード・同期ファイル・細工された入力によって、利用者の端末やデータが
  侵害される経路。

以下は**対象外**です:

- サードパーティ依存ライブラリの脆弱性 — 各プロジェクトの上流へ報告してください
  （[THIRD-PARTY-LICENSES.md](THIRD-PARTY-LICENSES.md) を参照）。依存関係の更新の
  ため、あわせてご連絡いただくのは歓迎します。
- Dropbox・Google Drive・OS 自身の認証情報ストレージの脆弱性 — 各ベンダーへ報告
  してください。
- 攻撃者がローカルアクセスや昇格した権限で既に端末を侵害していることを前提とする問題。
- 実証可能なセキュリティ影響のない、堅牢化の不足。

## セキュリティモデル（参考）

Keryx は攻撃対象領域を最小化するよう設計されています:

- **アカウントなし・開発者運営サーバーなし。** 開発者が管理するバックエンドは存在
  せず、アプリは購読フィードと、（利用者が有効にした場合のみ）Dropbox または
  Google Drive へ直接通信するだけです。
- **クラウド認証情報**（OAuth のアクセス／リフレッシュトークン）は OS の安全な
  認証情報ストレージ（macOS は Keychain、Windows は Credential Manager、Linux は
  Secret Service）に保存され、それが利用できない場合のみ、権限を制限した
  （`0600`）ローカルファイルにフォールバックします。
- **OAuth** は PKCE を用いた認可コードフローで、端末とプロバイダの間で直接行われ、
  認証情報が開発者のサーバーを経由することはありません。
- **ローカルデータ**（購読・キャッシュ記事・設定）は、クラウド同期を明示的に有効
  にしない限り端末内にとどまります。
- **アプリ内アップデートのダウンロードは検証していますが、発行者の身元まで認証しているわけでは
  ありません。** Keryx がアプリ内でアップデートのダウンロード・インストールを提案する場合
  （対応するプラットフォーム／インストール形態は `docs/background-update.ja.md` の
  「アプリ内アップデート」を参照）、ダウンロードしたファイルは、インストールする前に GitHub
  Releases API 自身がそのアセットについて報告する SHA-256 ダイジェストと照合します。通信は
  allowlist に登録された GitHub のホストへの HTTPS 接続に限られます。これにより転送経路の破損や
  転送中の改竄は検出できますが、**リリースを誰が発行したかまでは検証していません**——
  ダイジェスト自体、アセットの出所と同じ GitHub Releases API が計算・提供するものなので、
  Keryx のリリースを公開している GitHub アカウント／トークンがもし侵害されれば、すり替えられた
  アセットとそのダイジェストは互いに一致してしまいます。リリースの真正性に対する現在の信頼は、
  GitHub アカウントのセキュリティ（2FA、トークンのスコープ制限）と GitHub への HTTPS/TLS 通信に
  依拠しており、これは独立に検証可能なリリース署名を持たない他の多くのソフトウェア配布と同様です。
  より強い保証（例えば各リリースに添付する minisign/cosign の detached 署名と、アプリに埋め込んだ
  検証用公開鍵）は将来の改善候補として検討していますが、まだ実装していません。
- **macOS では、展開したアップデートを差し替える前に署名の自己整合性チェックも行います**
  （`codesign --verify --strict --deep`）。これは上記のダイジェスト照合とは独立に、展開後の
  バンドルの署名対象内容が署名後に改変・破損していないかを検出するものです。**発行者の身元を
  検証するものではありません**——現在のリリースビルドは Developer ID 証明書による署名・
  ノータライズではなく ad-hoc 署名のため、署名者を照合するための証明書チェーンが存在しません
  （`codesign --verify -R "anchor apple generic and certificate leaf[subject.OU] = <team id>"`
  を使うと、正規のリリースを含め ad-hoc 署名のリリースはすべて無条件に拒否されてしまいます）。
  実際に発行者を検証するチェックへの強化は、リリースに実在の Developer ID 署名とノータライズを
  導入した後に予定しています。同じ ad-hoc 署名という事情により、自己置換スクリプトは新しい
  バンドルを再起動する前に `com.apple.quarantine` フラグも（付いていれば）取り除きます——
  ad-hoc 署名では Gatekeeper が quarantine フラグを解除するための拠り所を持たないため、
  フラグが残っていると再起動がブロックされる可能性があるからです。
- **macOS のバンドルはインプロセスではなく `ditto` で展開します。** 署名済みバンドルの
  `CodeResources` は同梱 JDK 内のシンボリックリンクを*リンクとして*封印しており、
  `java.util.zip` には格納されたリンクと通常ファイルを見分ける手段がありません——そのため
  インプロセスの展開ではリンクが潰れ、上記のチェックが結果を拒否します。よって展開は
  `ditto -x -k` に委譲し、その前に `ZipExtractor.validate` を実行して、`ditto` 自身は上限を
  持たない展開に対しても zip slip・エントリ数・展開後サイズの上限を維持しています。格納された
  リンクの*リンク先*は事前検証できません（同じ死角）が、それを埋めるのは `ditto` 自身です
  （書庫は既に上記のダイジェスト照合を通過しています）: `..` を含むエントリ名を外へ出さず展開先へ
  正規化し、symlink を*経由した*書き込みはそもそも拒否して非ゼロ終了します。コード署名チェックは
  脱出に対する第 2 の防御線には**なりません**: バンドルディレクトリのみを検査するため、バンドルの
  *隣*に書かれたエントリは一度も見られず、また自己整合性チェックなので、書庫全体を生成できる
  攻撃者は自分のバンドルを ad-hoc 署名できてしまいます。これが検出するのはバンドル*内部*の
  改変であり、それがこのチェックの役割です。

データの取り扱いに関する完全な説明は [PRIVACY.ja.md](PRIVACY.ja.md) を参照してください。
